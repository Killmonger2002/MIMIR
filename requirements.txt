faster-whisper
piper-tts
openwakeword
sounddevice
pyaudio
numpy
thefuzz
pyautogui
pycaw
comtypes
pywin32
wmi
psutil
keyboard
pystray
Pillow
pyyaml
ollama
noisereduce
# Speaker verification (core/speaker_verify.py, enroll_voice.py) - unlike
# the wake-word training tooling, torch is a genuine runtime dependency
# here: resemblyzer's voice encoder loads every time MIMIR starts.
resemblyzer
torch
# Semantic UI control (core/ui_scanner.py + ui_executor) - reads the
# foreground window's UI Automation tree so "click Search" / "type X in
# the address bar" work by name, the way Windows Voice Access does.
pywinauto
